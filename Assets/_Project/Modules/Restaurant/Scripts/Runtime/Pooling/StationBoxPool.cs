using LabDiner.Shared.DesignPattern;

namespace LabDiner.Restaurant.Pooling
{
    public class StationBoxPool : SceneObjectPooling<StationBox>
    {
        // Nếu cần thêm logic đặc biệt cho StationBox khi trả về pool, có thể override ở đây
    }
}
